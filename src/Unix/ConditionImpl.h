#ifndef CONDITION_IMPL_H#define CONDITION_IMPL_H#include <pthread.h>class ConditionImpl {public:	/* Initialize a ConditionImpl object that will be able to	 * wait until var == value	 */	ConditionImpl(int& var);	~ConditionImpl(void);		/* Make a protected change to the value observed by the	 * ConditionImpl	 */	//void setValue(T& value);		void waitAndRetain(int value);	void release(void);	void signal(void);	private:	int& m_conditionnedVar;		pthread_cond_t m_cond;	pthread_mutex_t m_mutex;};#endif